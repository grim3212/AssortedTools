{
  "schemaVersion": 1,
  "id": "${mod_id}_gametest",
  "version": "${version}",
  "name": "${mod_name} Game Tests",
  "description": "Development-only gametests for ${mod_name}. Never built into a release artifact.",
  "authors": [
    "${mod_author}"
  ],
  "license": "${license}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.grim3212.assorted.tools.gametest.ToolsFabricGameTests"
    ]
  },
  "depends": {
    "fabricloader": ">=0.19",
    "${mod_id}": "*"
  }
}
